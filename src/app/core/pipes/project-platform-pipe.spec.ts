import { ProjectPlatformPipe } from './project-platform-pipe';

describe('ProjectPlatformPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectPlatformPipe();
    expect(pipe).toBeTruthy();
  });
});
